# MongoDB Connection String
# Replace with your actual MongoDB connection string
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>

# Server Port (optional - defaults to 4000 in server.js)
PORT=4000
